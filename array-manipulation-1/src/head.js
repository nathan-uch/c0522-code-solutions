/* exported head */

// delcare a variable to hold the final string
// assign the value at index 0 to the variable
// return the variable

function head(array) {
  var finalElement;
  finalElement = array[0];
  return finalElement;
}
