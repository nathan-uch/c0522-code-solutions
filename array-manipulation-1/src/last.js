/* exported last */

// declare a variable and assign the array at index equal to the total length of the array minus 1;
// return the variable

function last(array) {
  var lastEle = array[array.length - 1];
  return lastEle;
}
